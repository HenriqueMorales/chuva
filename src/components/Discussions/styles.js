import styled from "styled-components";

const StyledDiscussions = styled.section`
  border: 1px solid hsla(0, 0%, 93%, 1);
  border-radius: 4px;

  .create-topic-wrapper {
    display: flex;
    flex-direction: column;
    gap: 1.6rem;
    padding: 1.6rem;
  }
`;

export default StyledDiscussions;
